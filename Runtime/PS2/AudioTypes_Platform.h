/**
 * @file AudioTypes_Platform.h
 * @brief PS2 platform extension for the engine's `AudioTypes.h` fork.
 *
 * Phase 0-2 stub. SPU2 audio via libaudsrv (IRX module on the IOP) arrives
 * in Phase 3. No shared types need to leak into the engine layer yet.
 */

#pragma once
#include <stdint.h>
