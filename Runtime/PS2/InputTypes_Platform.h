/**
 * @file InputTypes_Platform.h
 * @brief PS2 platform extension for the engine's `InputTypes.h` fork.
 *
 * Phase 0-2 stub. DualShock2 / libpad bring-up arrives in Phase 3 alongside
 * the IRX module load on the IOP side. Engine's gamepad-only `#else`
 * fallback in InputConstants.h covers PS2 without needing an explicit arm.
 */

#pragma once
#include <stdint.h>
